import { TransformSlashePipe } from './transform-slashe.pipe';

describe('TransformSlashePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformSlashePipe();
    expect(pipe).toBeTruthy();
  });
});
